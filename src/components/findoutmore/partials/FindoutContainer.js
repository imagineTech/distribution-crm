//Core imports
import styled from 'styled-components';

//Invisible container for findout more landing page.
//This is to display extra space below the banner, as a placeholder till we render the subcomponents
const FindoutContainer = styled.div`
  text-align: center;
`

export default FindoutContainer;
