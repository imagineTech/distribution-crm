import Styled from 'styled-components';


//This is an invisible container in case we need to edit the layout for the about page
const AboutContainer = Styled.div`
  text-align: center;
`
export default AboutContainer;
