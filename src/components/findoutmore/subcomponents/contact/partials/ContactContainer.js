import Styled from 'styled-components';

//this is the main container for the contact component.
const ContactContainer = Styled.div`
  border: 2px solid blue;
  max-width: 1200px;
  margin: auto;
`

export default ContactContainer;
