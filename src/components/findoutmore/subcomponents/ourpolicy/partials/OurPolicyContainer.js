import Styled from 'styled-components';

//Contains all the styling for our policy component
const OurPolicyContainer = Styled.div`
  max-width: 1000px;
  margin: auto;
  font-size: 25px;
`

export default OurPolicyContainer;
