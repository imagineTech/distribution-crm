import React from 'react';

const ProductItem = ({ product, auth }) => {
    return (
      <div>
        <h1>Hello</h1>
        {console.log(auth)}
      </div>
    )
};

export default ProductItem;
